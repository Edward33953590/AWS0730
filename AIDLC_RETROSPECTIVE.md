# AIDLC 实践反思总结

> 项目：优惠券发放与核销中心  
> 团队：SRCG Workshop 竞赛团队  
> 时间：2026-07-29 ~ 2026-07-30

---

## 一、AIDLC 各阶段时间线

| 阶段 | 开始时间 | 结束时间 | 产出物 | 负责人 |
|------|----------|----------|--------|--------|
| 需求分析 | 07-29 上午 | 07-29 中午 | 4份需求文档 (功能需求24条 + 非功能需求10条 + 用户故事22条 + 需求清单) | 成员A、成员B |
| 系统设计 | 07-29 中午 | 07-29 下午 | 6份设计文档 (架构 + 数据库 + API规范 + 前端设计 + 技术栈 + 追踪矩阵) | 成员B、成员C |
| 任务规划 | 07-29 下午 | 07-29 下午 | 25个实现任务清单，按依赖链排序 | 成员B、成员C |
| 任务实现 | 07-29 下午 | 07-30 | 25个任务全部完成，覆盖完整业务闭环 | 成员C、成员D |
| 测试验证 | 07-29 晚间 | 07-30 | 端到端演示10步验证全部通过 | 成员E、全员 |

---

## 二、收获与亮点

### 1. 追踪矩阵让需求不遗漏
从需求到设计到实现的全链路追踪矩阵（traceability matrix），确保了24条功能需求每一条都有对应的设计方案和实现任务。在开发过程中多次通过矩阵发现遗漏点。

### 2. AI 降级设计在实际开发中多次生效
系统的4项AI功能（推荐、风控、文案、画像）全部设计了降级方案。在 Bedrock API 不可用的情况下，系统自动切换为规则引擎/模板方案，保证了核心业务流程不中断。这个设计在开发和演示中多次帮助我们应对 API 超时。

### 3. 阶段门禁保证质量
每个 AIDLC 阶段结束都有门禁检查（checklist），确保只有达标才能进入下一阶段。这避免了"带病上线"的情况，特别是需求阶段的门禁帮助我们发现了3个未明确的细节（核销方式、通知方式、有效期计算）。

### 4. 单机部署策略快速迭代
采用 Flask + SQLite 的轻量技术栈，实现了从需求到可运行系统仅用一天时间。无需配置外部数据库或复杂部署流程，开发即演示环境。

### 5. 7种优惠券类型的默认值设计
为每种优惠券类型设计了合理的默认参数，使运营人员在创建活动时可以"一键创建"，同时保留完全自定义的能力。这个设计来自需求阶段的用户调研。

---

## 三、遇到的挑战

### 1. 技术栈被迫迁移
原计划使用 Next.js + React + TailwindCSS + Prisma，但npm install持续超时无法完成。被迫在开发中途迁移到 Flask + Jinja2 + SQLAlchemy。教训：技术选型应有备选方案，且应在项目初期验证环境可用性。

### 2. 循环导入问题
Flask 应用中 `db = SQLAlchemy()` 定义位置不当导致循环依赖。所有 models 从 `app.py` 导入 db，而 app.py 又注册了使用 models 的 blueprints。解决方案：创建独立的 `extensions.py`，将所有扩展实例集中管理。

### 3. SQLite 并发限制
SQLite 在高并发场景下存在锁表问题。优惠券库存扣减需要原子操作，通过 raw SQL 的 `UPDATE ... WHERE remaining_stock > 0` 条件更新实现了并发安全，但牺牲了一些 ORM 的优雅性。

### 4. AI 响应延迟影响用户体验
Bedrock API 的响应时间在 2-8 秒不等，直接影响了用户操作的流畅度。通过异步前端请求（Alpine.js fetch）+ Loading 状态反馈 + 降级方案的组合策略缓解了这个问题。

### 5. 前端页面数量庞大
4种角色 × 多个功能页面 = 20+ 个 HTML 模板文件。在有限时间内逐一开发所有页面是最大的时间压力源。

---

## 四、如果重来会做什么

### 1. 更早验证技术栈
在需求阶段就完成技术栈的环境验证（安装依赖、跑通 Hello World），而不是在设计完成后才发现 npm 不可用。建议：在技术选型 ADR 中增加"环境验证"步骤。

### 2. 优先实现核心流程的端到端
先实现"创建活动→领券→核销"这条最短路径的完整闭环（包含前端），再逐步扩展。而不是先完成所有后端再做前端，这样可以更早获得演示反馈。

### 3. 引入组件化模板
Jinja2 模板中存在大量重复的卡片、表格、表单结构。如果使用 macro 或 include 组件化，可以显著减少重复代码，提高开发效率。

### 4. 更完善的测试策略
当前仅有端到端的手动验证流程。如果时间允许，应加入：
- API 自动化测试（pytest + Flask test client）
- 并发测试（模拟N+1领取）
- AI 降级测试（mock Bedrock 超时）

### 5. 数据埋点先行
AI 效果对比页面目前使用预设数据。如果在架构设计阶段就规划好数据埋点模型（推荐日志、点击追踪），就能在演示时展示真实数据，说服力更强。

---

## 五、下一步优化计划

### 短期（1-2天）
1. 引入 Redis 缓存层，解决风控内存计数重启丢失问题
2. 完善剩余占位符页面（风控监控、批量发券、模板管理）
3. 实现数据埋点系统（RecommendationLog 模型），积累真实 AI 效果数据

### 中期（1周）
4. 引入 Celery 异步任务队列，处理批量发券、通知推送等耗时操作
5. 完善 API 文档（Swagger/OpenAPI 格式），便于团队协作
6. 增加前端单元测试（Cypress 或 Playwright）
7. 移动端响应式适配

### 长期（1月+）
8. 迁移到 PostgreSQL，支持更好的并发性能
9. 支付对接（预留接口，不在竞赛范围）
10. 多租户架构支持，允许多个商户独立运营
11. A/B 测试框架，用于验证 AI 推荐的真实效果

---

## 六、团队协作模式总结

### 采用的实践
- **AIDLC 五步法**：需求 → 设计 → 规划 → 实现 → 验证
- **文档驱动开发**：先写文档后写代码，文档即设计
- **AI 辅助开发**：使用 Kiro + Bedrock 加速编码和文档生成
- **Git 版本管理**：功能分支 + 主分支合并
- **每日站会**：同步进度、暴露阻塞

### 效率数据
| 指标 | 数值 |
|------|------|
| 总开发时间 | ~16 小时 |
| 代码总行数 | ~9,200 行 |
| 文档总字数 | ~23,000 字 |
| 任务完成率 | 100% (25/25) |
| AI 功能数 | 4 项（推荐、风控、文案、画像） |
| 页面数量 | 22 个 |
| API 接口数 | 40+ |

---

## 七、可复用的方法论

### AIDLC 适用场景
- 竞赛类短周期项目（1-3天）
- 需求明确、边界清晰的功能开发
- 多人协作需要追踪进度的项目

### 关键 Takeaway
1. **追踪矩阵是核心**：需求↔设计↔实现的映射关系必须可查
2. **门禁检查不能省**：每个阶段的 checklist 是质量保证的底线
3. **降级方案先设计**：AI 等外部服务必须有 fallback plan
4. **技术验证前置**：选型后立即验证环境，不要等到开发阶段
5. **演示驱动开发**：始终以"能演示"为目标导向开发优先级

---

**文档结束**
